require 'spec_helper'

describe KoducStripe do
  it 'has a version number' do
    expect(KsStripe::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
